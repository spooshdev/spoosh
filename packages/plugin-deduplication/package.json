{
  "name": "@spoosh/plugin-deduplication",
  "version": "0.1.0",
  "description": "Request deduplication plugin for Spoosh - prevents duplicate in-flight requests",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nxnom/spoosh.git",
    "directory": "packages/plugin-deduplication"
  },
  "bugs": { "url": "https://github.com/nxnom/spoosh/issues" },
  "homepage": "https://github.com/nxnom/spoosh/tree/main/packages/plugin-deduplication#readme",
  "keywords": ["spoosh", "plugin", "deduplication", "api-client", "dedupe"],
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0",
    "format": "prettier --write 'src/**/*.ts'",
    "prepublishOnly": "npm run build && npm run typecheck && npm run lint"
  },
  "peerDependencies": {
    "@spoosh/core": ">=0.1.0"
  },
  "devDependencies": {
    "@spoosh/core": "workspace:*"
  }
}
